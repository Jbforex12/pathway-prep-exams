name: Keep exams warm

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping exams health
        run: curl -fsS --max-time 30 "https://exams.pathwayprep.online/health" | head -c 200
